# Community Map Examples

These examples are local-only.

- examples are for validation and dry-run learning
- do not put secrets in map files
- tokens must come from environment variables only
- execution is not enabled for community maps yet
