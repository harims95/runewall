# runewall/maps/

Installed Python package module used by the Runewall runtime and CLI for map validation, registry, execution, and community package logic.

This is not the same as the root-level `maps/` directory. Bundled site maps are in `runewall/maps/sites/`.
