# maps/

Project-level map assets directory. Intended for reference map files or examples that are not part of the installed Python package.

This directory is not imported by the Runewall runtime or CLI. The bundled runtime maps live in `runewall/maps/sites/`.
